package com.example.demo;

import java.io.FileNotFoundException;
import java.util.Scanner;

public class Program {	
/*public static void main(String[] args) throws FileNotFoundException
{
	Client client=new Client("mo","r" ,"r" ,"r" );
	client.register();
	if(client.login())
	{
	
	}
	Driver driver=new Driver("ka","x" ,"x" ,"x", "x","x" );
	driver.register();
	Admin admin=Admin.getInstance();
	admin.verifyDriver(driver);
	if(driver.login())
	{
		Area area=new Area("maadi");
		driver.AddFavoriteArea(area);
		System.out.println("a7a");
		System.out.println(driver.FavoriteArea.size());
	}
	Area area2=new Area("mm");
	if(client.login())
	{
		Area area1=new Area("maadi");
		client.requestRide(area1, area2, 4);
	}
	if(driver.login())
	{
		System.out.println(driver.FavoriteArea.get(0)+"------");
		driver.ListRides();
		Ride ride=driver.rides.get(0);
		Offer offer=new Offer(1000);
		driver.offer(ride,offer);
		driver.arrivedToUserLocation(ride);
	}
	if(client.login())
	{
		System.out.println(client.offers.get(0).price);
		FileBase f=FileBase.getInstance();
		client.accOff(new Offer( 1000));
		Area area=new Area("maadi");
		Area area1=new Area("uu");
	}
	if(driver.login())
	{
		Ride ride=driver.rides.get(0);
		driver.arrivedToUserDest(ride);
		admin.showEvents(ride);
	}
	if(client.login())
	{
		Area area=new Area("maadi");
		Area area1=new Area("uu");
	}
}**/}

